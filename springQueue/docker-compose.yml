services:
  spring-app:
    build: .
    container_name: spring-kafka-container
    ports:
      - "8080:8080"
    environment:
      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:29092
    networks:
      - kafka-infra-net

networks:
  kafka-infra-net:
    external: true
    name: kafka_back-net